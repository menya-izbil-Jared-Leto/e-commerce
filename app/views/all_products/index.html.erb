<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= all_products_path() %>">Главная</a>
  <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
      <li class="nav-item">
          <%= link_to "Orders", carts_path, class:"carts"%>
      </li>
      <li class="nav-item">
          <%= link_to "My profile", root_path, class:"profile"%>
      </li>
      </ul>
  </div>
  </nav>
<div class="product_header">
  <h1> PRODUCTS <h1>
</div>
<hr/>
<% @products.each do |product| %>
  <div class = "product_image-container">
    <p><%= product.name %></p> 
    <%= link_to product.name, product_path(product), class: "product_detail-btn" %>
     </div> 
     <br/>
    <% end %>
</body>